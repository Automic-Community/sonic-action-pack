*** Settings ***
Suite Setup       Startup
Suite Teardown    Teardown
Force Tags        delete_vlan
Test Template     Delete VLAN Template
Library           com.automic.robot.itpa.ItpaLibrary
Library           String
Library           DateTime
Resource          ../../Resources/messages.txt
Resource          ../../Resources/keywords.txt
Resource          ../../Resources/variables.txt
Resource          ../../Resources/actions.txt

*** Test Cases ***    OK_NOK    VLANID
Delete VLAN with existing vlan id
                      [Tags]    main_scenarios
                      OK        ${VLAN_ID}

Delete VLAN with invalid vlan id
                      NOK       Invalid

*** Keywords ***
Startup
    Log    *******Start Test "DELETE VLAN"*******
    Log    *******Connect to AE*******
    Connect AE

Delete VLAN Template
    [Arguments]    ${OK_NOK}    ${VLANID}
    [Documentation]    This action tests the feature of Delete VLAN.
    ...    -${OK_NOK}: Boolean value indicate if the action is ENDED_OK or ENDED_NOT_OK. Value of this variable should be OK or NOK.
    ...    -${VLANID}: This field specify ID of the VLAN to be deleted.
    Generate String
    Init Action    ${DELETE_VLAN_ACTION}
    Connect using SSH
    Run Keyword If    '${VLANID}' == 'Invalid'    Action Set    &UC4RB_SONIC_VLAN_ID#    ${Generated_Name}    ELSE    Action Set
    ...    &UC4RB_SONIC_VLAN_ID#    ${VLANID}
    Action Execute
    Run Keyword If    '${OK_NOK}' == 'OK'    Assert Success    ELSE    Assert Failure

Teardown
    Log    *******End Test "DELETE VLAN"*******
